import { LockKeyhole, Mail } from 'lucide-react';
import { useState, type FormEvent } from 'react';
import { api } from '../services/api';
export default function Login({onSuccess,onBack}:{onSuccess:(token:string)=>void;onBack:()=>void}){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{const r=await api.adminLogin(email,password);localStorage.setItem('mqcd_admin_token',r.token);onSuccess(r.token)}catch(e){setError(e instanceof Error?e.message:'Não foi possível entrar.')}finally{setLoading(false)}}return <section className="login-page"><div className="login-art"><div><span>MEMÓRIAS QUE CONSTROEM DEMOCRACIA</span><h1>Juntos, construímos<br/><em>um território de memória.</em></h1></div></div><div className="login-card"><button className="back-link" onClick={onBack}>← Voltar</button><span className="eyebrow">ÁREA DA EQUIPE</span><h2>Entrar na conta</h2><p>Acesse a área de moderação e publicação do Atlas.</p><form onSubmit={submit}><label><Mail/>E-mail<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="admin@atlas.local"/></label><label><LockKeyhole/>Senha<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required placeholder="••••••••"/></label>{error&&<div className="form-error">{error}</div>}<button className="button primary full" disabled={loading}>{loading?'ENTRANDO...':'ENTRAR'}</button></form><div className="login-help">Demo local: <strong>admin@atlas.local</strong> / <strong>atlas2026</strong></div></div></section>}
