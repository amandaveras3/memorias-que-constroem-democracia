import { CalendarDays, MapPin, UserRound, X } from 'lucide-react';
import type { City, Memory } from '../types';
export default function MemoryModal({memory,city,onClose,onOpen}:{memory:Memory|null;city?:City;onClose:()=>void;onOpen:()=>void}){if(!memory)return null;return <div className="modal-backdrop" onMouseDown={onClose}><article className="memory-modal" onMouseDown={e=>e.stopPropagation()}><button className="close-button" onClick={onClose}><X/></button><div className="memory-modal-cover" style={{backgroundImage:`url(${memory.image||'/assets/Fundo.png'})`}}/><div className="memory-modal-body">{memory.demo&&<span className="demo-badge">DADO DE DEMONSTRAÇÃO</span>}<span className="memory-category">{memory.category}</span><h2>{memory.title}</h2><div className="memory-meta"><span><MapPin/>{city?.name} · {memory.neighborhood}</span><span><CalendarDays/>{memory.period}</span><span><UserRound/>{memory.contributor}</span></div><p>{memory.story}</p><button className="button primary" onClick={onOpen}>Abrir página completa</button></div></article></div>}
